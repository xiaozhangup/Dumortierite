# Dumortierite - clj-module

clojure lib designed to provide setup for main component.

## compile

change into parent dir (Dumortierite root dir) and execute:

```shell
make
```

the final jar will be assembled in `assemble/`
